# 서버 정밀 리뷰 설계 메모

## 현재 구현 범위

`POST /api/review-analysis`는 저장된 기보에서 1부터 시작하는 실제 수 번호(`ply`)를 받아 다음 두 장면을 서버 NNUE로 분석합니다.

1. 실제 수를 두기 직전: 최선 수와 평가를 구합니다.
2. 실제 수를 둔 직후: 같은 300ms 조건으로 평가를 다시 구합니다.

요청 예시:

```json
{ "revision": 2, "ply": 1 }
```

응답의 핵심 구조:

```json
{
  "side": "cho",
  "playedMove": "a4b4",
  "recommendedMove": "a4b4",
  "match": true,
  "beforeFen": "...",
  "recommendedFen": "...",
  "budgetMs": 300,
  "source": "local-server",
  "analysis": {
    "before": {
      "depth": 14,
      "evaluation": { "unit": "cp", "sideToMove": 5, "cho": 5 },
      "pv": ["a4b4", "i7h7"]
    },
    "after": {
      "depth": 13,
      "evaluation": { "unit": "cp", "sideToMove": -8, "cho": 8 },
      "pv": ["i7h7", "h1g3"]
    },
    "rawLossCp": 3,
    "lossCp": 3,
    "lossReason": null,
    "terminalOutcome": null
  }
}
```

## 평가 손실의 의미

- 모든 비교는 `evaluation.cho`를 사용해 초 관점으로 통일한 뒤, 실제로 둔 진영의 관점으로 손실 부호를 계산합니다.
- `rawLossCp`는 `착수 전 평가 - 착수 후 평가`를 둔 사람의 관점으로 나타냅니다.
- 짧은 탐색 결과가 흔들려 `rawLossCp`가 음수가 될 수 있습니다. 화면용 `lossCp`는 이때 0으로 제한하지만 원값은 진단과 향후 보정에 쓰도록 보존합니다.
- `cp`는 Fairy-Stockfish의 내부 평가 단위이며 공식 장기 기물 점수가 아닙니다.
- 실제 수가 대국을 끝내면 착수 후 검색을 생략하고 `lossReason`을 `terminal`로 반환합니다.
- `beforeFen`과 `recommendedFen`은 클라이언트가 좌표 문구 대신 추천 수를 판 위에서 재생할 때 사용합니다.
- `prediction.moves`와 `prediction.fens`는 엔진 PV를 서버 규칙으로 재검증한 예상 수순과 각 중간 장면입니다.
- 두 평가 중 하나가 없으면 `analysis-unavailable`, 하나라도 강제 승패인 `mate` 평가이면 `mate-score`를 반환합니다. 이 경우 cp 손실을 임의로 만들지 않습니다.

## 실험적 수 등급

`classification`은 `key`, 한국어 `label`, `experimental`, `basis`를 반환합니다. 현재 임시 기준은 다음과 같습니다.

| 조건 | 등급 |
| --- | --- |
| 엔진 1순위와 일치 | 최선 |
| 손실 0~15cp | 매우 좋음 |
| 손실 16~40cp | 좋음 |
| 손실 41~80cp | 부정확 |
| 손실 81~160cp | 실수 |
| 손실 161cp 이상 | 큰 실수 |
| cp 비교 불가 | 분류 제외 |

이 경계는 제품 흐름을 검증하기 위한 임시값입니다. 장기 승리 기대값 데이터로 보정하기 전에는 사용자 실력을 단정하는 기준으로 사용하지 않습니다.

## 제약과 결정이 필요한 부분

- 일반 수 하나는 엔진을 두 번 순차 실행하므로 현재 조건에서 약 600ms와 프로세스 실행 비용이 듭니다.
- 300ms 고정 시간 탐색은 장면과 서버 부하에 따라 깊이가 달라지며 평가가 흔들릴 수 있습니다.
- `lossCp`만으로 좋은 수·부정확·실수·블런더를 나누는 기준은 아직 없습니다. 실제 장기 기보와 더 긴 기준 탐색으로 임계값을 보정해야 합니다.
- 강제 승패 전환을 cp와 별도 등급으로 처리할 정책이 필요합니다.
- 현재는 요청마다 엔진 프로세스를 실행합니다. 전체 기보 리뷰 전에 작업 큐, 동시 실행 제한, 결과 캐시, 취소와 재시도 구조가 필요합니다.
- 현재 `MultiPV=1`이라 최선 수만 제공합니다. 후보 수 여러 개를 보여 주려면 비용 측정 후 MultiPV 확장을 결정해야 합니다.

## 다음 구현 후보

### 전체 기보 리뷰 작업 API

- `POST /api/review-start`: 현재 revision의 전체 기보 리뷰를 시작하고 `jobId`, `completed`, `total`, `status`를 즉시 반환합니다.
- `POST /api/review-status`: `jobId`의 진행률과 현재까지 완성된 수별 결과를 반환합니다.
- `POST /api/review-cancel`: 실행 중인 엔진 검색을 중단합니다.
- 상태는 현재 서버 메모리에만 보관됩니다. 서버 재시작 후 복원과 영구 캐시는 후속 범위입니다.
- 현재 서버당 전체 리뷰 하나만 실행하며 같은 작업을 다시 시작하면 기존 작업 상태를 반환합니다.
- 완료 응답의 `summary`에는 등급별 수, 핵심 수, cp 비교 가능 수의 평균 손실이 포함됩니다. 정확도 점수는 아직 생성하지 않습니다.

1. 전체 리뷰 작업의 서버 재시작 복원과 위치 평가 영구 캐시
2. 같은 위치와 분석 조건의 결과 캐시
3. Godot 복기 화면의 한 수 분석, 추천 수 판 위 재생과 Tween 기물 이동은 구현 완료. 다음에는 애니메이션의 실기기 체감과 저사양 성능을 확인
4. 수집 기보와 장시간 탐색을 이용한 수 등급 임계값 실험
