# 서버 정밀 리뷰 설계 메모

## 이번 구현 범위

`POST /api/review-analysis`는 저장된 기보에서 실제 착수 하나를 선택해, 그 수를 두기 직전 위치를 서버 NNUE로 300ms 분석합니다.

요청:

```json
{ "revision": 2, "ply": 1 }
```

`ply`는 기보와 같은 1부터 시작하는 수 번호입니다. 응답은 당시 진영, 실제 수, 추천 수, 일치 여부, 엔진 출처와 탐색 예산을 반환합니다.
분석은 기보·revision·저장 파일을 변경하지 않으며, 추천 수를 당시 합법 수로 다시 검증합니다. 자동 AI 응수가 실행 중이면 CPU 탐색 충돌을 피하기 위해 거부합니다.

`analysis`에는 다음 정보가 포함됩니다.

```json
{
  "depth": 14,
  "selDepth": 15,
  "nodes": 74609,
  "nps": 247870,
  "timeMs": 301,
  "evaluation": { "unit": "cp", "sideToMove": 5, "cho": 5 },
  "pv": ["a4b4", "i7h7", "h1g3"]
}
```

- `evaluation.sideToMove`: 분석 시점에 둘 차례인 진영 관점의 UCI 평가.
- `evaluation.cho`: 모든 장면을 비교하기 쉽도록 초 관점으로 부호를 통일한 평가.
- `unit=cp`는 엔진 내부 우세 단위이며 장기의 공식 기물 점수가 아닙니다.
- `unit=mate`이면 값의 부호와 수로 강제 승패까지의 거리를 나타냅니다.
- `pv`는 추천 수부터 시작하는 엔진의 예상 진행 수순입니다.

## 아직 정밀 리뷰가 아닌 이유

- 현재 비교는 실제 수와 최선 수가 같은지와 두기 전 평가만 알려줍니다. 실제 수를 둔 뒤의 평가가 없어 좋은 수·실수·치명적 실수로 분류할 수 없습니다.
- 최선 수의 변화도(PV)는 반환하지만 대안 수는 반환하지 않고 `MultiPV=1`만 사용합니다.
- 요청마다 엔진 프로세스를 새로 실행하며 HTTP 작업 큐 안에서 약 300ms를 기다립니다.
- 한 판 전체 분석, 진행률, 취소, 결과 캐시와 재시작 복구가 없습니다.
- 같은 위치라도 짧은 시간 탐색 결과가 달라질 수 있어 일치 여부만으로 수의 품질을 단정하면 안 됩니다.

## 다음 설계에서 결정할 내용

1. 실제 수를 둔 다음 위치도 같은 조건으로 분석해 평가 손실을 계산할지, MultiPV에서 실제 수를 강제 분석할지.
2. 전체 기보 작업을 HTTP 요청과 분리한 단일/제한된 worker queue, 작업 ID, 진행률, 취소와 캐시 구조.
3. 장기 점수 범위에 맞춘 수 분류 기준과 빅장·반복·점수승 같은 종료 규칙의 표기 방식.
4. 무료 기기 고전 평가와 유료 서버 NNUE 결과가 다를 때 사용자에게 품질 차이를 설명하는 방식.

현재 API는 위 결정을 하기 전에 UI 흐름과 서버 비용을 측정하기 위한 최소 기반입니다.
