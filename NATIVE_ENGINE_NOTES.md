# Android 기기 엔진 기술 검토 메모

## 자유 분석 형세 평가

- 검색 결과에 초 관점 `evaluation_unit`과 `evaluation_cho`를 추가해 자유 분석 점수 패널에서 사용합니다.
- 현재 `Use NNUE=false`인 고전 평가이며 서버 NNUE cp와 직접 비교하거나 합산하지 않습니다.
- NNUE 파일의 Android 패키징, 로딩 경로, 메모리와 발열 측정은 아직 해결되지 않았습니다. 이 전까지 UI에 `기기 고전평가`로 표시합니다.

실기기 연결 중 확정하지 못했거나 후속 검토가 필요한 내용을 기록합니다.

## 초기화가 끝나지 않은 현상 — 원인 확인

- 환경: SM-F741N, Godot 4.7.2, Fairy-Stockfish `226c7f18`, ARM64, 고전 평가.
- 최초 관찰에서는 탐색이 멈춘 것으로 보였으나 단계별 로그를 추가한 결과 `Bitboards::init()`에서 실행 중이었음.
- 원인: 공식 Makefile이 기본으로 정의하는 `PRECOMPUTED_MAGICS`가 GDExtension 빌드에 빠져, 시작할 때 이동 테이블용 magic 값을 직접 찾고 있었음.
- 앱 강제 종료로 검색을 중단했으며 사용자 기보는 변경하지 않음.
- 조치: 공식 빌드와 같이 사전 계산 테이블을 사용하고 `movetime = 300`, `depth = 8`을 함께 적용해 어느 제한이든 먼저 도달하면 종료하도록 함.
- 남은 검증: 첫 초기화 시간, 반환 수 합법성, 연속 호출, 탐색 중 취소를 실기기에서 확인해야 함.

## Android 전용 GDExtension 내보내기 진단

- Windows Godot 내보내기 과정에서 Android 라이브러리만 선언한 `.gdextension`을 스캔하며
  `No GDExtension library found for current OS and architecture (windows.x86_64)` 오류를 출력함.
- 내보내기는 성공했고 APK에는 ARM64/x86_64 라이브러리와 descriptor가 올바른 위치에 포함됨.
- 현재 빌드 스크립트는 descriptor를 Android 내보내기 직전에 생성하고 직후 제거해 일반 데스크톱 실행에는 영향을 주지 않음.
- 기능상 차단 문제는 아니지만, 오류 없는 배포 파이프라인을 위해 Windows 스텁 라이브러리 또는 Godot의 플랫폼별 확장 패키징 방식을 추가 검토할 필요가 있음.

## 이번 실기기 검증에서 남긴 항목

- 완료: ARM64 확장 로딩, 현재 3수 기보 재생, 연속 추천 2회, 서버 합법 수 검증, 추천 전후 revision·기보 불변 확인.
- 측정: 같은 위치에서 `b10c8`, 깊이 8, 순수 탐색 34ms와 45ms. 첫 호출의 엔진 초기화 시간은 따로 계측하지 않음.
- 미검증: 탐색 중 취소. 현재 탐색이 수십 ms에 끝나 adb 좌표 입력으로 안정적으로 중간 상태를 만들기 어려움.
- 미검증: 복기 장면 추천과 앱 백그라운드 전환 중 취소. 자동 실기기 테스트 진입점을 만든 뒤 검증하는 편이 안전함.
- UI 메모: 초/한 선택 상자는 현재 대국 진영 표시가 아니라 다음 `새 AI 대국` 설정값이지만, 현재 상태처럼 보여 혼동될 수 있음.
