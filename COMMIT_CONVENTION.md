# 커밋 규칙

## 형식

```text
type(scope): 한국어 요약

필요한 경우 변경 이유와 검증 결과
```

- type은 소문자 영문, scope는 선택 사항입니다.
- 제목은 72자 이내로 작성하며 끝에 마침표를 붙이지 않습니다.
- 한 커밋에는 하나의 논리적 작업을 담습니다. 기능 코드와 해당 테스트·진행 기록은 함께 커밋합니다.
- 기존 1~3단계는 실제 현재 상태를 하나의 초기 커밋으로 남깁니다. 과거 단계의 이력을 만들어내지 않습니다.

## 종류

| type | 용도 |
| --- | --- |
| feat | 새로운 기능 |
| fix | 오류 수정 |
| docs | 문서 변경 |
| refactor | 동작을 유지하는 코드 구조 개선 |
| test | 테스트 추가·수정 |
| perf | 성능 개선 |
| build | 의존성·빌드 설정 |
| ci | 자동 검증 설정 |
| chore | 저장소 설정 등 유지보수 |
| revert | 이전 커밋 되돌리기 |

scope 예: `game`, `ai`, `rules`, `ui`, `history`, `repo`.

```text
feat(history): 기보 저장과 복기 이동 추가
fix(ai): 취소된 탐색 결과의 착수 방지
docs: 다음 작업과 검증 결과 갱신
```

호환성이 깨지는 변경은 `feat(api)!: ...`처럼 표시하고 본문에 `BREAKING CHANGE:`와 변경 사항을 설명합니다.

## 작업 절차

1. 작업 시작 시 `git status`와 `PROGRESS.md`를 확인합니다.
2. 작고 검증 가능한 작업 단위를 구현하고 관련 검증을 실행합니다.
3. `PROGRESS.md`에 완료 범위·검증 결과·남은 작업을 갱신합니다.
4. diff와 스테이징 목록을 확인하고 해당 작업의 파일만 커밋합니다.
5. 원격 연결 후 완료한 커밋을 push하고 로컬·원격 동기화를 확인합니다.

기본 브랜치는 `main`입니다. 사용자 변경을 임의로 되돌리거나 커밋에 섞지 않습니다.
공유한 이력에 강제 push 또는 임의의 amend를 하지 않습니다.
`.local/`, 엔진 실행 파일, 대국 백업, `node_modules/`, IDE 설정, 인증 정보와 `.env`는 커밋하지 않습니다.
검증하지 못한 항목과 push 실패는 완료로 기록하지 않습니다.
