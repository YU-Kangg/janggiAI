# 누적 10개 작업 실기기 테스트 목록

## 네 번째 누적 10개 작업 (7단계 AG~AP)

- [x] 리뷰 요약에 평가 비교 가능 수가 표시된다.
- [x] 엔진 1순위와 일치한 최선수 일치율이 표시된다.
- [x] 초·한별 핵심 수와 평균 손실이 표시된다.
- [x] 전체 리뷰의 평균 탐색 깊이가 표시된다.
- [x] 착수 전·후 검색을 합산한 엔진 시간이 표시된다.
- [x] 최대 손실 수의 번호·진영·cp가 표시된다.
- [x] `최대 손실 수` 버튼이 해당 복기 장면으로 이동한다.
- [x] 기보 항목에 등급과 cp 손실이 함께 표시된다.
- [x] 현재 복기 수의 등급과 손실이 장면 이동에 맞춰 갱신된다.
- [x] 서버 검색 시간과 장기 기보 보정 전이라는 실험 기준 안내가 표시된다.

네 번째 누적 테스트 대상 커밋: `6aed61e`~`d3cfb9e`
현재 상태: 2026-09-22 SM-F741N / Android 16에서 10개 항목 통과

검증 기록:

- APK: v2/v3 서명 확인, ARM64, min SDK 24 / target SDK 36, 약 61.6MB
- 새 전체 리뷰: 5수, 최선 3·매우 좋음 1·좋음 1, 비교 가능 5수, 최선수 일치 60%
- 탐색 요약: 평균 깊이 14, 엔진 합계 3.0초, 평균 손실 13cp
- 진영 요약: 초 3수·핵심 0수·평균 6cp, 한 2수·핵심 0수·평균 23cp
- 최대 손실: 2수 한 35cp이며 버튼 이동 후 기보와 현재 수 상세가 같은 값을 표시
- 앱 복원: 강제 종료 후 새 요약, 실험 기준 안내와 형세 그래프 유지
- Android 로그: GDScript 오류, `FATAL EXCEPTION`, `AndroidRuntime` 오류 없음
- 화면 기록: `.local/batch4-summary.png`, `.local/batch4-worst.png`, `.local/batch4-restored.png` (로컬 전용)

---

## 세 번째 누적 10개 작업 (7단계 W~AF)

- [x] 초·한 진영별 리뷰 수와 평균 손실이 요약에 표시된다.
- [x] 선택한 수에 깊이·노드·분석 시간이 표시된다.
- [x] 핵심 장면 등급 필터와 이동 버튼이 실기기 화면에서 활성 상태를 반영한다.
- [x] 다시 두기와 다음 실수 연습 버튼이 리뷰 상태에 맞게 표시된다.
- [x] 리뷰 JSON 저장·불러오기 버튼이 완료 리뷰에서 활성화된다.
- [x] 앱 재실행 후 로컬 리뷰가 자동 복원된다.
- [x] 복원된 리뷰의 그래프·요약·기보 등급이 유지된다.
- [x] 리뷰 JSON 수별 무결성 검증의 정상 파일 경로가 앱 복원에서 통과한다.
- [x] 그래프에서 수를 선택하면 추천 수 판 재생과 기물 이동 경로가 오류 없이 실행된다.
- [x] 예상 수순·기보 재생용 직전 수 강조 기능이 포함된 APK가 정상 시작하고 리뷰 화면을 렌더링한다.

세 번째 누적 테스트 대상 커밋: `998fdd2`~`307d56f`
현재 상태: 2026-09-21 SM-F741N / Android 16에서 설치·연결·복원·추천 재생 통과

검증 기록:

- APK: v2/v3 서명 확인, ARM64, min SDK 24 / target SDK 36, 약 61.6MB
- 대국/리뷰: revision 121, 5수, 최선 3·부정확 2·핵심 장면 2·평균 손실 37cp
- 앱 복원: 강제 재실행 후 형세 그래프·요약·기보 `[최선]` 표시 유지
- Android 로그: GDScript 오류, `FATAL EXCEPTION`, `AndroidRuntime` 오류 없음
- 화면 기록: `.local/batch3-start-device.png`, `.local/batch3-bottom.png`, `.local/batch3-review4.png` (로컬 전용)
- 손상 JSON 거부는 자동 Godot 통합 테스트로 검증했으며 Android 앱 저장소에 변조 파일을 주입하는 검사는 생략

---

## 두 번째 누적 10개 작업 (7단계 M~V)

- [x] 예상 수순을 재생하다 `수순 정지`를 누르면 현재 장면에서 멈춘다.
- [x] `수순 이전·다음`으로 예상 수순 장면을 한 단계씩 이동한다.
- [x] 전체 리뷰 후 기보 목록의 각 수에 `[최선]`, `[부정확]` 같은 등급이 표시된다.
- [x] `이전 핵심 수`와 `다음 핵심 수`가 현재 위치 기준의 핵심 장면으로 이동한다.
- [x] 핵심 장면에서 `현재 순서/전체 개수`가 정확히 표시된다.
- [x] `기보 재생`이 시작 배치부터 마지막 수까지 진행한다.
- [x] `기보 정지` 후 다음 재생 간격이 지나도 현재 수가 유지된다.
- [x] 느림·보통·빠름 선택이 예상 수순과 기보 재생 양쪽에 적용된다.
- [x] 완료된 대국에서 `전체 리뷰`를 다시 눌렀을 때 즉시 기존 결과가 표시된다.
- [x] 앱을 종료 후 다시 실행하거나 서버에 다시 연결하면 그래프·요약·수 등급이 복원된다.

두 번째 누적 테스트 대상 커밋: `6156d12`~`c8a6263`
현재 상태: 2026-09-18 SM-F741N / Android 16에서 10개 항목 통과

검증 기록:

- APK: `c8a6263` 기능까지 포함한 디버그 빌드, v2/v3 서명 확인
- 전체 리뷰: 5수, 최선 3·부정확 2·핵심 장면 2·평균 손실 37cp
- 서버 재사용: 재요청 전후 `jobId: 1`, `status: complete` 유지
- 앱 복원: 강제 종료 후 다시 실행해 요약·핵심 장면·기보 `[최선]` 표시 복원
- Android 로그: 스크립트 오류와 치명적 예외 없음
- 화면 기록: `.local/batch2-reviewed.png`, `.local/key-first.png`, `.local/key-second.png`, `.local/key-back.png`, `.local/pv-stop.png`, `.local/pv-next.png`, `.local/pv-prev2.png`, `.local/game-stop1.png`, `.local/game-stop2.png`, `.local/review-restored.png` (로컬 전용)

---

대상 기기: SM-F741N 또는 Android 7.0/API 24 이상의 ARM64 기기  
대상 기능: 실기기 카운트 1~10의 리뷰 기능

## 준비

- [x] 휴대폰 USB 디버깅을 켜고 PC 연결을 허용한다.
- [x] `adb devices -l`에서 기기 상태가 `device`인지 확인한다.
- [x] 최신 `main`에서 전체 자동 테스트가 통과했는지 확인한다.
- [x] 최신 Android APK를 빌드하고 v2/v3 서명을 검증한다.
- [x] APK를 업데이트 설치한다.
- [x] 최신 Node 서버를 재시작한다. 저장된 대국이 복원되는지 확인한다.
- [x] `adb reverse tcp:3000 tcp:3000`을 설정하고 앱을 실행한다.
- [ ] 최소 6수 이상이며 일부러 좋지 않은 수를 포함한 테스트 대국을 준비한다.
- [x] 화면을 위아래로 스크롤해 장기판 아래의 모든 리뷰 버튼에 접근할 수 있다.

## 1. 전체 기보 백그라운드 리뷰

- [x] `전체 리뷰`를 누르면 즉시 앱 조작이 돌아오고 분석이 백그라운드에서 시작된다.
- [x] 서버 연결이 끊기거나 앱이 멈추지 않는다.
- [x] 완료 후 원본 기보의 수와 현재 대국이 변경되지 않는다.

## 2. 실험적 수 등급

- [ ] 각 분석 수에 `실험 등급` 문구가 표시된다.
- [ ] 엔진 추천과 같은 수는 `최선`으로 표시된다.
- [ ] 좋지 않은 테스트 수는 손실에 따라 `부정확·실수·큰 실수` 중 하나로 표시된다.
- [ ] 종료 또는 강제 승패처럼 cp 비교가 불가능한 수가 앱 오류를 일으키지 않는다.

## 3. 전체 리뷰 진행률과 취소

- [ ] 분석 중 `완료 수/전체 수`가 증가한다.
- [ ] `리뷰 취소`를 누르면 작업이 취소되고 앱이 계속 동작한다.
- [ ] 취소 후 `전체 리뷰`를 다시 시작해 정상 완료할 수 있다.

## 4. 전체 리뷰 결과 재사용

- [ ] 완료 후 이전 수·다음 수로 이동하면 등급과 손실이 즉시 바뀐다.
- [ ] 같은 수에서 추천 재생을 반복해도 새 서버 분석 대기 시간이 생기지 않는다.
- [ ] 복기 이동만으로 현재 대국의 revision이나 기보가 바뀌지 않는다.

## 5. 형세 그래프

- [x] 전체 리뷰 완료 후 평가 선과 0점 기준선이 보인다.
- [x] 복기 이동 시 현재 수의 노란 점이 함께 이동한다.
- [x] 그래프의 앞·중간·뒤를 눌렀을 때 대응하는 복기 장면으로 이동한다.
- [x] 세로 화면에서 그래프가 장기판이나 버튼을 가리지 않는다.

## 6. 다음 핵심 수

- [ ] `다음 핵심 수`가 현재 위치 뒤의 첫 `부정확·실수·큰 실수` 장면으로 이동한다.
- [ ] 핵심 수가 더 없으면 버튼이 비활성화된다.
- [ ] 자유 분석 중에는 버튼이 비활성화된다.

## 7. 다시 두기

- [ ] 분석된 1수 이후 장면에서 `다시 두기`를 누르면 해당 수 직전 장면으로 돌아간다.
- [ ] 추천 수를 직접 두면 성공 안내가 나온다.
- [ ] 다른 합법 수를 두면 더 좋은 수가 있다는 안내가 나온다.
- [ ] `재개`를 누르면 원래 선택한 복기 장면으로 돌아간다.
- [ ] 다시 두기 수가 원본 기보에 저장되지 않는다.

## 8. 단계별 힌트

- [x] 다시 두기에서 첫 `힌트`를 누르면 움직일 기물의 칸만 노란색으로 강조된다.
- [ ] 두 번째 `힌트`를 누르면 도착 칸이 초록색으로 추가 강조된다.
- [ ] 안내 문장에 좌표나 정답 수가 직접 노출되지 않는다.
- [ ] 한 수를 둔 뒤에는 힌트 버튼이 비활성화된다.

## 9. 예상 수순 자동 재생

- [ ] `예상 수순`을 누르면 시작 장면부터 여러 수가 약 0.55초 간격으로 재생된다.
- [ ] 좌표 목록 대신 장기판의 기물 이동으로 이해할 수 있다.
- [ ] 재생 중 다른 복기 장면 또는 자유 분석으로 이동하면 이전 재생이 즉시 멈춘다.
- [ ] PV가 짧거나 종료되는 장면에서도 앱 오류가 없다.

## 10. 전체 리뷰 요약

- [x] 리뷰 완료 후 등급별 수 개수가 표시된다.
- [x] 각 등급 개수의 합이 전체 기보 수와 일치한다.
- [x] cp 비교 가능한 수가 있으면 평균 손실이 표시된다.
- [x] 화면 폭이 좁아도 요약 문구가 잘리기보다 줄바꿈된다.

## 기존 기능 회귀 확인

- [ ] 초와 한 선택 AI 대국, 착수, 한수쉼, 무르기가 정상 동작한다.
- [ ] 기기 추천과 추천 취소가 정상 동작한다.
- [ ] 자유 분석에서 초·한 양쪽 수, 분기 무르기, 재개가 정상 동작한다.
- [ ] 자유 분석 수마다 기물 점수와 `기기 고전평가`가 갱신된다.
- [ ] USB를 뺐다가 다시 연결한 후 `run-android.ps1 -SkipInstall`로 서버 연결이 복구된다.

## 결과 기록

- 테스트 일시: 2026-09-17
- APK 커밋: `be645a8`
- 기기/Android 버전: Samsung SM-F741N / Android 16 (API 36)
- 통과 항목: 준비 8개, 전체 리뷰 3개, 형세 그래프 4개, 첫 단계 힌트 1개, 리뷰 요약 4개
- 실패 항목과 재현 순서: 최초 APK에서 하단 버튼에 접근할 수 없었음. 전체 콘텐츠에 세로 스크롤을 추가한 `be645a8` APK로 해결함.
- 미확인 항목: 5수 기보로 검사했으므로 6수 준비 조건, 취소·결과 재사용·다음 핵심 수·다시 두기 정답/오답·두 번째 힌트·예상 수순·기존 기능 회귀는 다음 수동 검사에서 확인한다.
- 화면 또는 로그 파일: `.local/scroll-controls2.png`, `.local/review-complete-ui.png`, `.local/graph-start.png`, `.local/graph-end.png`, `.local/hint-one.png` (로컬 전용)
